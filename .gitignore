# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite / TS
node_modules/.tmp

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Résumé PDF: kept locally, deliberately not published to the site or the repo.
# The pipeline uses the markdown mirror (tools/job-radar/candidate-resume.md) instead.
tools/job-radar/candidate-resume.pdf
